// Google OAuth Button

const googleBtn = document.getElementById("googleLogin");

googleBtn.addEventListener("click", function () {

    alert(
        "Google OAuth Login will be integrated in the backend.\n\nThis is a frontend demonstration."
    );

});