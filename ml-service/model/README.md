# Place your trained scikit-learn model files here:
#
# - skin_model.pkl   (trained RandomForestClassifier or RandomForestRegressor)
# - pipeline.pkl     (ColumnTransformer + OneHotEncoder Pipeline)
#
# The ML service will automatically load these files on startup.
# If files are not found, the service runs in DEVELOPMENT MODE
# using a heuristic-based assessment.
