$kind: http-request
description: Update fields supported by your AssessmentUpdate Pydantic schema. Adjust the sample body if your schema uses different fields.
url: "{{base_url}}/api/assessment/{{assessment_id}}"
method: PUT
headers:
  Authorization: Bearer {{token}}
  Accept: application/json
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "identified_concerns": ["acne", "dark_spots"],
      "prioritized_concerns": ["acne", "dark_spots"],
      "risk_factors": ["Poor sleep"],
      "condition_score": 82
    }
order: 8000
